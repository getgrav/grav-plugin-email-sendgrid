{
    "name": "getgrav/email-sendgrid-tests",
    "description": "Test harness for the Email SendGrid plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin stays free of development packages.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5",
        "psr/http-message": "^1.1 || ^2.0"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailSendgrid\\": "../classes/",
            "Grav\\Plugin\\EmailSendgrid\\Tests\\": "./"
        }
    },
    "config": {
        "sort-packages": true
    }
}
